import { registerElement } from '@nativescript/core';

export function registerComponents() {
    // Register custom components here if needed
    // Example: registerElement('CustomComponent', () => require('./CustomComponent').CustomComponent);
}